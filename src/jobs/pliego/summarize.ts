/**
 * Moved to shared/pliego/summarize so the Nitro on-demand endpoint can generate
 * summaries too. Re-exported here for the existing job entry (pliego-summary.ts).
 */
export * from "../../../shared/pliego/summarize";
